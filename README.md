# AthleteProfileInputForm

fork/clone this repo

type "npm install"

navidate to the src3 folder

type in "nodemon server/server.js" in the command line or terminal

open the file "index.html" in the src3 folder
